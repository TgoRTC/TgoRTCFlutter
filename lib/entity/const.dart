enum RTCType {
  audio,
  video;
}

enum ConnectStatus {
  connecting,
  connected,
  disconnected,
}

enum TgoCameraPosition {
  front,
  back,
}

enum TgoConnectionQuality {
  unknown,
  excellent,
  good,
  poor,
  lost,
}
